language: generic 
sudo: required
services: 
  - docker

before_install:
  - docker build -t volkovd/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true volkovd/docker-react npm run test